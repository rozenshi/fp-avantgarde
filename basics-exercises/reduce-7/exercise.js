// filter: (a -> Boolean) -> Array a -> Array a
const filter = predicate => arr => { /*TODO*/ }

// map :: (a -> b) -> Array a -> Array b
const map = transformer => arr => { /*TODO*/}

// some :: (a -> Boolean) -> Array a -> Boolean
const some = predicate => arr => {/*TODO*/}

// every :: (a -> Boolean) -> Array a -> Boolean
const every = predicate => arr => {/*TODO*/}

// includes :: a -> Array a -> Boolean
const includes = a => arr => {/*TODO*/}
